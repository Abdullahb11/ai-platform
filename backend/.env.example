# Gemini API Configuration
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# Database Configuration
DATABASE_URL=postgresql+asyncpg://ai_platform_user:abdullah@localhost:5432/ai_platform

# Application Context Window (tokens) — application-level limit, not Gemini's native limit
APP_CONTEXT_WINDOW_TOKENS=1000
